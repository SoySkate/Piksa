import User from '@/components/Modals/User';
import React, { createContext, useContext, useState } from 'react';



// const [userList, setUserList] = useState<User[]>([]);
export default function Data(u:User){

   
    return(
        <div></div>
    )
}